apiVersion: rbac.authorization.k8s.io/v1beta1
kind: ClusterRole
metadata:
  name: kubeadm-detector
  labels:
    app: kubeadm-detector
rules:
- apiGroups: [""]
  resources:
  - nodes
  verbs: ["get", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1beta1
kind: ClusterRoleBinding
metadata:
  name: kubeadm-detector
  labels:
    app: kubeadm-detector
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kubeadm-detector
subjects:
- kind: ServiceAccount
  name: kubeadm-detector
  namespace: kube-system
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kubeadm-detector
  namespace: kube-system
  labels:
    app: kubeadm-detector
---
apiVersion: extensions/v1beta1
kind: DaemonSet
metadata:
  name: kubeadm-detector
  namespace: kube-system
  labels:
    app: kubeadm-detector
spec:
  template:
    metadata:
      labels:
        name: kubeadm-detector
    spec:
      tolerations:
      - key: node.cloudprovider.kubernetes.io/uninitialized
        operator: Exists
        effect: NoSchedule
      - key: node-role.kubernetes.io/master
        operator: Exists
        effect: NoSchedule
      serviceAccountName: kubeadm-detector
      containers:
      - image: busybox
        command:
        - /bin/sh
        - -c
        - 'while true; do kubectl annotate --overwrite --token=$(cat /var/run/secrets/kubernetes.io/serviceaccount/token) nodes $NODE_NAME cloud.appscode.com/kubeadm-version=$(kubeadm version -o short); sleep 300; done'
        imagePullPolicy: IfNotPresent
        name: busybox
        env:
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        volumeMounts:
        - mountPath: /usr/bin/kubeadm
          name: kubeadm
          readOnly: true
        - mountPath: /usr/bin/kubectl
          name: kubectl
          readOnly: true
      volumes:
      - name: kubeadm
        hostPath:
          path: /usr/bin/kubeadm
      - name: kubectl
        hostPath:
          path: /usr/bin/kubectl
