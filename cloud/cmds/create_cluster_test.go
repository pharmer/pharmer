package cmds

// TODO:
// add e2e test: runs pharmer create cluster.... command with all possible options
// compare created cluster, machine, machineset yamls with actual data
// question: should this be in unit tests?
// currently we're only testing if files are created or not, not comparing file contents
